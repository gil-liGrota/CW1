s1 =  "Hello"
s2 =  "World"

#1. s1 + s2
# s1 and s2 will be one string without space between them:
# HelloWorld

#2. s1 + i (i = 3)
# s1 will double by i and be one string without space between them:
# HelloHelloHello

#3. s1.find(s2)
# the function return 0 if s2 is into s1 and -1 if it don't show
# -1


#4. s1 < s2
# the function return True if the alphabetical order of s2 is bigger than s1
# and False if not
# True


#5. s1.lower()
# converts s1 into lower case
# hello

#6. s1.upper()
# converts s1 into upper case
# HELLO

#6. s1.lower[x:y] (x = 1, y = 3)
# the function "cut" the word between x and y and return the cut:
# el

#ASCII:
#ASCII table is representing chars by numbers so we could compare between chars
#1. ord('a') - ord('A') = 97 - 65 = 32

#2. 'a' < 'B' = 97 < 66 = False